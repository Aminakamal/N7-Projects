void increment(ref int n, int times) {
    if (times < 1) {
        return;
    } else {
        n = (n+1);
        increment(ref n, (times+(-1))); 
        return;
    }
}

test {
    int x = 0;
    increment(ref x, 5);
    print x;
}
